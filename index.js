/**
 * @format
 */

import {AppRegistry} from 'react-native';
import DotAnimation from './index';
import {name as appName} from './app.json';

// AppRegistry.registerComponent(appName, () => DotAnimation);

export default DotAnimation;
